# HAT Sample

`HAT3D.SL` は、`GRAPHF.LIB` / MAGIC を使ったグラフィックサンプルです。

固定小数点サンプルのグラフィックデモを作る際、`@INIT()` や `@LINEC()` の使い方を確認する参照元として利用しています。

## 注意

このサンプルは、MZ-2500 向けの MAGIC/GRAPHF 環境を前提にしています。

現時点では未発表の MZ-2500 用 MAGIC ライブラリを使って動作確認しているため、公開済みの SLANG クロスコンパイラ一式だけでは、そのままビルド・実行できない場合があります。
